#include <iostream>;
#include <vector>;


using std::vector;
using std::cin;
using std::cout;
using std::endl;

int main(){

	int sizeRow = A.size();
	int sizeCol = A[0].size();


	vector<vector<int>> result (i, vector<int> (sizeRow, 0));

	for(int i = 0; i < sizeRow; i++) {
		for (int j = 0; j < sizeCol; j++ )
		{

				result[j][i] = A[i][j]
		
	}

	
	return result;
}